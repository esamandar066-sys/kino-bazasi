## Packages
framer-motion | Page transitions and scroll-triggered animations
date-fns | Human-readable date formatting

## Notes
Assuming @shared/routes exports the `api` object and `buildUrl` function as specified in the routes_manifest.
Assuming @shared/schema exports `MovieResponse` and `InsertMovie`.
Replit Auth is handled via window.location.href = '/api/login' and the provided useAuth hook.
